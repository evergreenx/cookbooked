import React from "react";

const CustomInput = () => {
  return (
    <div>
      <input
        className="lg:w-[30%] w-full "
        placeholder="Add one or mutiple steps "
      />
    </div>
  );
};

export default CustomInput;
